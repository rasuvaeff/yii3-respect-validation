# Examples

| Script | Shows | Needs server? |
|--------|-------|:-------------:|
| [`validate-form.php`](validate-form.php) | Wrapping Respect rules with `#[RespectRule]` on a DTO property, reading `Result` errors | no |

## Running

From the package root, after `composer install`:

```bash
php examples/validate-form.php
```
